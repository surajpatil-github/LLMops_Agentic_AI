apiVersion: v1
kind: Service
metadata:
  name: multidocchat-app
  namespace: multidocchat
  labels:
    app: multidocchat-app
spec:
  type: ClusterIP
  selector:
    app: multidocchat-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
